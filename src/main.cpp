#include <iostream>

#include "systems/terminal.hpp"
#include "core/app.cpp"

// Should start a bunch off process
// Maybe preload some settings and then start the app
int main() {
    App().run();

    return 0;
}